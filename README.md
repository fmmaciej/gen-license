# README

## Usage

```bash
Usage: install.sh <command> 

Commands:
  install
  uninstall
  status
  help

Installs to:
  ${INSTALL_DIR}

Symlink:
  ${LINK_PATH} -> ${INSTALL_DIR}/${ENTRYPOINT_REL}
```
